language: python

python:
  - 3.6.6

services:
  - postgresql  

before_script:
  - psql -c 'create database test_db;' -U postgres -W 12345
  
install:
  - pip install -r requirements.txt
  - pip install coveralls

script: 
  - pytest --cov-report term-missing --cov=app

after_success:
- coveralls